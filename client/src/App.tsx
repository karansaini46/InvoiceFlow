import "./styles.css";

export function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">InvoiceFlow</p>
        <h1>Billing workflows, ready to build.</h1>
        <p className="intro">
          A React client and Express server are running from one pnpm workspace.
        </p>
      </section>
    </main>
  );
}
